import * as path from "node:path";

export default path.resolve(import.meta.dirname, "..");

// TODO: it feels lonely in here...
// TODO: should I call this file "at.ts", just like @ in @/path/to/file? (related: see downloadImage)
